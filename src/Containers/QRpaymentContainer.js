import {connect} from 'react-redux'
import ScreenQRpayment from '../Screens/ScreenQRpayment'
const mapStateToProps = state =>({

})
const mapDispatchToProps ={

}
export default connect(
    mapStateToProps,
    mapDispatchToProps,
)(ScreenQRpayment)