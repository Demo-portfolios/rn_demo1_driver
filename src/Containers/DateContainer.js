import {connect} from 'react-redux'
import ScreenDate from '../Screens/ScreenDate'
const mapStateToProps = state =>({

})
const mapDispatchToProps ={

}
export default connect(
    mapStateToProps,
    mapDispatchToProps,
)(ScreenDate)