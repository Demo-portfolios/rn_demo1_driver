import {connect} from 'react-redux'
import ScreenDel_correct from '../Screens/ScreenDel_correct'
const mapStateToProps = state =>({

})
const mapDispatchToProps ={

}
export default connect(
    mapStateToProps,
    mapDispatchToProps,
)(ScreenDel_correct)