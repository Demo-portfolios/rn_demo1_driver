import {connect} from 'react-redux'
import ScreenCorrect from '../Screens/ScreenCorrect'
const mapStateToProps = state =>({

})
const mapDispatchToProps ={

}
export default connect(
    mapStateToProps,
    mapDispatchToProps,
)(ScreenCorrect)